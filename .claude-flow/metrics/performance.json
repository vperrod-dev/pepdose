{
  "timestamp": "2026-07-27T13:15:53.005Z",
  "mode": "headless",
  "workerType": "optimize",
  "model": "sonnet",
  "durationMs": 53270,
  "executionId": "optimize_1785158099735_i10se7",
  "success": true,
  "findings": {
    "sections": [],
    "codeBlocks": [
      {
        "language": "ts",
        "code": "const PEPTIDE_BY_ID = new Map(PEPTIDES.map(p => [p.id, p]));\nexport function getPeptideById(id: string) { return PEPTIDE_BY_ID.get(id); }"
      }
    ]
  },
  "rawOutputPreview": "Audit already done, file exist: `tasks/perf-audit-2026-07-27.md`. Re-verified against source — still accurate. Summary:\n\n**1. Sync full-scan every 30s (biggest)** — `src/db/sync.ts:166` `db.getAll(kind)` on all 6 IndexedDB stores every tick (`AuthGate.tsx:45`, 30s interval), remote side already delta-filtered but local ignore it. No `by-updatedAt` index exists. Fix: add index, use `getAllFromIndex` with `IDBKeyRange.lowerBound(delta)`.\n\n**2. `getPeptideById` O(n) scan** — `src/data/peptides.ts:752`, linear `PEPTIDES.find()`, called inline in JSX render loops (Dashboard, Calendar, HalfLife, Protocols, VialInventory — confirmed 15+ call sites). Fix: swap to module-level `Map`, zero call-site changes:\n```ts\nconst PEPTIDE_BY_ID = new Map(PEPTIDES.map(p => [p.id, p]));\nexport function getPeptideById(id: string) { return PEPTIDE_BY_ID.get(id); }\n```\n\n**3. No `useMemo` on derived data** — Dashboard enrichment map, InjectionMap zoneStats, Symptoms topNames recompute every render. Calendar already do it right (`dosesByDate` useMemo) — pattern just needs copy-paste to others.\n\n**4. No shared query cache across page mounts** — Dashboard/Calendar/HalfLife independently refetch overlapping protocol/dose data on nav. Low urgency (local IndexedDB, not network) — deferred.\n\nChecked, clean: no N+1 DB query pattern (operations.ts all indexed lookups), no memory leaks (interval/listener cleanup correct in AuthGate + notifications.ts).\n\nFull detail + priority order in `tasks/perf-audit-2026-07-27.md`.\n",
  "rawOutputLength": 1507
}