# Copy to .env.local and fill in from your Supabase project:
#   Supabase dashboard -> Project Settings -> API
# Leaving these unset keeps pepdose fully local (no login, no cloud) — same as before.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
